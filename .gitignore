node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example
data/
db.json

# Local wallet balance snapshots (personal, generated by scripts/wallet_status.mjs)
wallet-status.json
wallet-status-history.jsonl

# External review/design bundles — reference only, must never build or deploy.
# Kept out of the repo so they can't pollute typecheck or ship to the live site.
reference/
!scripts/reference/
!scripts/reference/**

# Recovery proof corpora are preserved locally and referenced by hashes. Commit
# compact proof manifests and screenshots, not multi-hour JSONL series or stale
# process-coordinate pointers.
output/proof/flywheel-recovery/soak-*/
output/proof/flywheel-recovery/*.path
output/working_memory/fast-perp-research-batches/
